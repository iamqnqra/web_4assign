<h2>Welcome <% if (user) { %> 
    <%= user.username %> !    
    <img src="<%= user.avatar %>" alt="User Avatar" class="avatar">
<% } %></h2>

<% if (!user) { %>
    <div class="welcome-container">
        <p>We're glad to have you here.</p>
        <p>Login or <a href="/register" class="highlight">Register</a> to continue.</p>
        <p>If you already have an account, <a href="/login" class="highlight">Login</a> instead.</p>
        <p>Join our community and explore amazing features tailored just for you!</p>
    </div>
<% } else { %>
    <form action="/upload-avatar" method="POST" enctype="multipart/form-data">
        <label for="avatarInput" class="upload-label">Choose an image:</label>
        <input type="file" name="avatar" id="avatarInput" required>
        <button type="submit" class="upload-button">Upload an image</button>
    </form>    
<% } %>

